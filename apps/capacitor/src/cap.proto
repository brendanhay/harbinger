message SubReq {
  required bytes queue = 1;
  optional bool ack = 3;
  optional uint32 offset = 4;
}

message PubResp {
  required bytes topic = 1;
  required bool success = 2;
  optional uint32 offset = 3;
}

message Payload {
  required bytes content_type = 1;
  required bytes content = 2;
}
